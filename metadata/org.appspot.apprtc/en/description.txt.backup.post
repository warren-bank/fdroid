Copied from the official WebRTC source code repo (tag: 1.0.28513).

Uses WebRTC Native APIs via JNI.

Features include the ability to:

make WebRTC video calls using a free public signaling server (https://appr.tc)
directly connect 2 devices on the same LAN without the need for a signaling server